$('.question-errors').html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
  $('#question-<%= @question.id %>').replaceWith('<%= j render @question %>');
  $('.errors').html('');
<% end %>

$(document).trigger('embedGist')
